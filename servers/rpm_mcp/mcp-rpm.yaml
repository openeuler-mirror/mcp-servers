name: "rpm"
summary: "MCP server for querying rpm info"
description: |
  Provides MCP tools for querying rpm info.

dependencies:
  system:
    - python3
    - uv
    - python3-mcp
    - python3-requests
  packages:
    - rpm

files:
  required:
    - mcp_config.json
    - src/server.py
